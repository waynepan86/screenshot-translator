# 截图工具 {{version}}

一款 Windows 截图翻译工具：框选屏幕，识别并翻译文字，将译文显示在原文位置，方便阅读外文网页、软件界面和文档。

## 快速开始

1. 按 **{{region}}** 框选区域，或按 **{{fullscreen}}** 截取全部屏幕。
2. 点击工具栏“翻译”，等待译文直接显示在原图上。小幅纠偏自动在后台完成，不弹出复核窗口。
3. 点击“复原”切换回原图。在截图窗口内**按住空格**临时查看原图，松开恢复译文。
4. 按 Enter 或点击确认，将当前结果复制到剪贴板；Esc 取消。保存按钮可以另存为图片。
5. 点击“贴图”或按 F3，将当前截图或译文置顶，并退出截图编辑。拖动移动，滚轮缩放，Ctrl+滚轮调整透明度，按住空格查看原图，双击或 Esc 关闭；右键可复制图片和恢复原始大小。

区域截图时，鼠标悬停可高亮窗口，单击直接选中；拖动仍然手动框选。支持窗口级选择，不自动选择按钮等控件。

工具栏“遮盖打码”可拖动绘制不透明色块，使用当前颜色。遮盖会一起进入复制、保存和贴图结果，按住空格查看原图也保留遮盖；后续识别输入也包含遮盖。已发送的翻译请求无法撤回，请先打码再翻译。Ctrl+Z 可以撤销。

画笔、矩形、箭头和文字工具支持标注；Ctrl+Z 撤销标注。移动或调整选区后，识别及译文会清空，需要重新识别。

## 复核与修改文字

点击 OCR 入口打开“文字复核与翻译对照”，从上方下拉框选择文本块。

- **原始识别**：保留 OCR 首次读到的文字，供对照。
- **复核结果**：可编辑。修改后点击“应用并翻译”，只重新翻译当前块。
- **待核对**：置信度偏低或可能拼写异常；不代表一定有错，专有名词可能被标记。
- **已纠错**：局部图像两次重识别一致且满足保守纠错条件。仍建议核对关键内容。
- **恢复原始识别**：把编辑框恢复为首次识别文字，再点击“应用并翻译”生效。
- **重试本块**：跳过翻译缓存重新请求；翻译失败时可使用。
- **此块显示原图**：恢复这一块的原始像素；重试可再次显示译文。
- 下方可复制全部复核后的原文或全部译文。编辑框尚未应用的改动不会进入复制结果。

小幅纠偏自动在后台进行，无需设置。只对最多 6 个可疑区域进行额外识别，在时间预算用完后停止启动新区域；当前一次识别仍需完成。英文词典仅用于发现可疑词，代码、网址、路径、数字和大小写混合标识符受到保护。本版不包含大模型语义改写。只有主动点击“提取文字”才会打开文字面板。

## 原位显示

译文优先在原文本块内排版，适度缩小字号。放不下或无法可靠分离文字与背景时保留原图，完整译文仍显示在面板中，避免覆盖相邻内容或丢字。

深色界面支持亮色文字。复杂照片、旋转文字、很小或模糊的字体仍可能识别或擦除不理想，建议缩小选区、放大源页面再截图，或在面板中修正。

## 配置 DeepL

托盘右键 → 设置 → 翻译引擎选择 DeepL → 填写 Auth Key → 测试连通性 → 保存。

免费版密钥以 `:fx` 结尾，程序自动选择对应接口。翻译会发送识别出的文字和同一截图的相关上下文给 DeepL，不发送截图图片。首选引擎失败时可能发送文字给备用 Google / MyMemory；面板会明确标明实际引擎和失败原因。

翻译方向按整张截图的文字判断：中文为主译成英文，其余译成中文。多文本批量请求保持文本块对应，内存缓存减少重复请求；退出应用后缓存清空。

密钥保存在程序目录的 `config.json`，请勿公开分享该文件。GitHub 源码仓库与发布包不包含你的密钥。更新程序时保留原有 `config.json`；若旧文件在源码目录，可将它复制到新版 exe 同目录。

## 常见问题

- **某些文字没变化**：可能是数字、路径、原文与译文相同、翻译失败或原位空间不足；查看该块状态。
- **翻译慢**：首次识别需要加载模型；网络失败会等待超时并尝试备用引擎。
- **快捷键无效**：可能被其他软件占用，在设置中改用其他组合。
- **退出截图**：后台请求结束后会释放资源，不会把结果写进下一次截图。

## 当前版本功能

支持后台小幅纠偏、原位翻译与原图对照、逐块编辑重译、贴图置顶、窗口选择及遮盖打码。工具栏采用白底、深色图标、细边框和柔和阴影。

平滑背景支持整段擦除和四边渐变重建，减少行内代码底框残留及逐行接缝；无法可靠修复时保留原图。应用介绍与版本号可在托盘右键 → 关于中查看。

1.10.3 修复紧排三行大标题的 OCR 行框少量重叠导致分组跳行的问题，减少标题译文重叠和下划线残留。

1.10.4 调整截图初始遮罩：框选前直接透出真实桌面，避免桌面截图重放使 ClearType 字体边缘显得模糊；拖动框选时再切换到冻结画面。

1.10.5 修复透明窗口高亮区域的鼠标穿透：保持框选前字体清晰，同时恢复单击窗口选择和拖动框选。

1.10.6 修复框选完成后的字体模糊：原文区域继续直接显示真实桌面，冻结图仅用于识别、翻译和导出。

